// CONCATENATION
var name = "gia";
var message = "hello";

// alert(message + " " + "name");

// LENGTH - number of characters in a string
var name = "guillermo";
name.length;

console.log(name.length);
